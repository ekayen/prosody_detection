#!/bin/bash

#for f in $(find /afs/inf.ed.ac.uk/group/project/prosody/burnc -name '*.sph' -or -name '*.spn'); do
#    echo $f
#    sox -t sph $f -b 16 -t wav "${f%.*}.wav"
#done

#for f in $(find /afs/inf.ed.ac.uk/group/project/prosody/burnc -name '*.wav'); do
#    mv $f burnc/
#
#done

for f in burnc/*.wav; do
#    if [[ ! -f "${f%.*}.csv" ]]; then
	./SMILExtract -C config/IS13_ComParE.edit.conf -I $f -csvoutput "${f%.*}.is13.csv"
#    fi
done


